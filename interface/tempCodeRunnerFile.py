                # for f in self.model_features:
                #     f_clean = f.lower.replace(",", "").replace("(", "").replace(")", "").strip()
                #     if med_lower == f_clean or med_lower in f_clean or f_clean in med_lower:
                #         example.loc[0, f] = 1.0